PUSH1 0x05
CALLVALUE
GT
ISZERO
RET_WORD

; ISZERO(GT(callvalue, 5)) is folded into callvalue <= 5 (ult callvalue, 6).
; CHECK: [[CMP:%.*]] = icmp ult i256 {{.*}}, 6
; CHECK: select i1 [[CMP]]
; CHECK: store i256
