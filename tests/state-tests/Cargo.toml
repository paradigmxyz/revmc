[package]
name = "revmc-state-tests"
version = "0.0.0"
publish = false
edition = "2021"

[dependencies]
revmc = { workspace = true, features = ["llvm"] }

revm = { workspace = true, features = ["std"] }
revm-statetest-types = "14.0"

eyre.workspace = true
serde_json = "1.0"
walkdir = "2.5"

[lib]
path = "src/lib.rs"
